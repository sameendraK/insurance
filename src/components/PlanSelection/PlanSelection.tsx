import React, { FC } from 'react';
import { PlanSelectionWrapper } from './PlanSelection.styled';

interface PlanSelectionProps {}

const PlanSelection: FC<PlanSelectionProps> = () => (
 <PlanSelectionWrapper>
    PlanSelection Component
 </PlanSelectionWrapper>
);

export default PlanSelection;
